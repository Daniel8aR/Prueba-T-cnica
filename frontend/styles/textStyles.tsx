import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    title: {
        fontSize: 28,
        textAlign: 'center',
        marginBottom: 20,
    },

    infoText: {
        fontSize: 20,
        textAlign: 'center',
        color: '#333',
        marginBottom: 10,
        fontWeight: '300', // ligero, no negrita
    },
});