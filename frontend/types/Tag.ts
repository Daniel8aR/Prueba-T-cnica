// src/types/Tag.ts

// Se define la interfaz Tag, que representa una etiqueta que se puede asociar a una tarea.
export interface Tag {
  // Cada etiqueta tiene un id único, un nombre descriptivo y un color asociado.
  id: number;
  name: string;
  color: string;
}
